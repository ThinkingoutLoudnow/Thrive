<%= form_with(model: object_thrife, local: true) do |form| %>
  <% if object_thrife.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(object_thrife.errors.count, "error") %> prohibited this object_thrife from being saved:</h2>

      <ul>
      <% object_thrife.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
